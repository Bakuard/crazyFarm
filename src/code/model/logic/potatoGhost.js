'use strict'

module.exports.PotatoGhost = class PotatoGhost {
    constructor(timeInSeconds) {
        this.timeInSeconds = timeInSeconds;
    }
};

module.exports.potatoGhostSystem = function potatoGhostSystem(groupName, world) {
    
};